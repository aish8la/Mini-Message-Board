<ul>
    <% messages.map(message => { %> 
        <li>
            <span><%= message.text %></span>
            <span><%= message.user %></span>
            <span><%= message.added %></span>
        </li>
    <% }) %>
</ul>